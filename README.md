# Storefront Backend Project

This project is a backend API for an online storefront built with Node.js, Express, TypeScript, and PostgreSQL.

## Project Setup

### 1. Clone the project
```bash
git clone <your-repo-link>
cd storefront-backend